package com.rudux.dddd.view.mongodb

import reactivemongo.api.commands.CollectionCommand

import scala.concurrent.ExecutionContext

trait MongoActionHelpers {

//  implicit def mapToUnit[R, E <: Effect](action: CollectionCommand)(implicit ec: ExecutionContext): CollectionCommand =
//    action.map(_ => ())

}
